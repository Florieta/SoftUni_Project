@using RentalCarManagementSystem.Core.Models.Admin

@model LocationViewModel

@{
    ViewBag.Title = "Edit location";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="card card-warning">
    <div class="card-header">
        <h3 class="card-title">Edit Location Form</h3>
    </div>
    <div class="card-body">
        <form method="post">
            <div class="row">
                <div class="col-sm-6">
                    <div class="form-group">
                        <label asp-for="@Model.LocationName">Location Name</label>
                        <input asp-for="@Model.LocationName" type="text" class="form-control" placeholder="Enter Location Name..." />
                        <span asp-validation-for="LocationName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.Address">Address</label>
                        <input asp-for="@Model.Address" type="text" class="form-control" placeholder="Enter Address..." />
                        <span asp-validation-for="Address" class="text-danger"></span>
                    </div>
                </div>
            </div>
            <div class="mb-3">
                <input class="btn btn-primary" type="submit" value="Add" />
            </div>
        </form>
    </div>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    }

